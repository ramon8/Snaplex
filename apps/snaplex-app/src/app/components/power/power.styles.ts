import styled from "styled-components";
import { Text } from './../text'
import { Power } from "./power";

export const ContainerPower = styled(Text)`
    /* display: grid;
    place-items: center; */
    font-size: 16px;
    
    color: white;
    border: 1px solid transparent;
`